import "./App.css";
import Header from "./components/layout/Header";
import Index from "./components/layout/Index";
import Footer from "./components/layout/Footer";

const App = () => (
  <>
    <Header />
    <Index />
    <Footer />
  </>
);

export default App;

const arr = [1, 2, 4, 3, 4, 5, 6, 7, 3];

const unique = (arr) => {
  let newArr = [];

  for (let i = 0; i < arr.length; i++) {
    const element = arr[i];
  }
};
